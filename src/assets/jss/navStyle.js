const navStyle = {
	navBar: {
		height: '60px',
		backgroundColor: 'var(--bg-accent)',
		display: 'flex',
		justifyContent: 'flex-end',
		alignItems: 'flex-end',
	},
	navItems: {
		color: 'var(--disabled)',
	},
};

export default navStyle;
