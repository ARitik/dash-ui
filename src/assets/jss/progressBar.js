const progressBarStyle = {
	primaryBar: {
		padding: '8px',
		width: '80%',
		borderRadius: '20px',
		backgroundColor: 'var(--disabled)',
		color: 'var(--primary)',
	},
};

export default progressBarStyle;
